local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Erin's Universal & Bodycam Hub",
   LoadingTitle = "Loading Mission Exploits...",
   LoadingSubtitle = "by NotAmnesia",
   ConfigurationSaving = { Enabled = true, FolderName = "ErinCombat" }
})

-- SERVICES
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

-- SETTINGS
local StealthSettings = {
    Aimbot = false,
    FOV = 150,
    Sensitivity = 0.15, -- Lower = faster/snappier
    AimPart = "Head",
    TeamCheck = false
}

-- [THE PERFECT AIMBOT]
local function GetClosest()
    local nearest = nil
    local lastDist = StealthSettings.FOV
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild(StealthSettings.AimPart) then
            if StealthSettings.TeamCheck and v.Team == LocalPlayer.Team then continue end
            local pos, onScreen = Camera:WorldToScreenPoint(v.Character[StealthSettings.AimPart].Position)
            if onScreen then
                local dist = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)).Magnitude
                if dist < lastDist then
                    lastDist = dist
                    nearest = v
                end
            end
        end
    end
    return nearest
end

RunService.RenderStepped:Connect(function()
    if StealthSettings.Aimbot then
        local target = GetClosest()
        if target then
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, target.Character[StealthSettings.AimPart].Position), StealthSettings.Sensitivity)
        end
    end
end)

--- [TABS] ---

-- COMBAT TAB (Aimbot Only)
local CombatTab = Window:CreateTab("Combat", 4483362458)
CombatTab:CreateToggle({
    Name = "Aimbot Enabled",
    CurrentValue = false,
    Callback = function(v) StealthSettings.Aimbot = v end
})

CombatTab:CreateSlider({
    Name = "Aimbot FOV",
    Range = {50, 800},
    Increment = 10,
    CurrentValue = 150,
    Callback = function(v) StealthSettings.FOV = v end
})

-- BODYCAM MISSION MASTER
local PlaceId = game.PlaceId
if PlaceId == 16404660684 or PlaceId == 15358669610 or PlaceId == 16587913123 then
    local BodycamTab = Window:CreateTab("Bodycam Missions", 4483362458)

    BodycamTab:CreateSection("Mission Cheats")
    
    BodycamTab:CreateButton({
        Name = "Infinite Battery/Flashlight",
        Callback = function()
            -- Attempting to find local battery values
            for _, v in pairs(game:GetDescendants()) do
                if v.Name:lower():find("battery") or v.Name:lower():find("flashlight") then
                    if v:IsA("NumberValue") or v:IsA("IntValue") then v.Value = 9999 end
                end
            end
        end
    })

    BodycamTab:CreateButton({
        Name = "Instant Interaction",
        Callback = function()
            -- Speeds up hold-E interactions
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("ProximityPrompt") then
                    v.HoldDuration = 0
                end
            end
        end
    })

    BodycamTab:CreateButton({
        Name = "Fullbright (See Everything)",
        Callback = function()
            game.Lighting.Ambient = Color3.new(1, 1, 1)
            game.Lighting.ColorShift_Bottom = Color3.new(1, 1, 1)
            game.Lighting.ColorShift_Top = Color3.new(1, 1, 1)
        end
    })

    BodycamTab:CreateSection("Teleports")
    BodycamTab:CreateButton({
        Name = "TP to Mission Objective",
        Callback = function()
            -- Automatically finds items named 'Objective' or 'Key'
            for _, v in pairs(workspace:GetDescendants()) do
                if v.Name:lower():find("key") or v.Name:lower():find("objective") or v.Name:lower():find("tool") then
                    if v:IsA("BasePart") then
                        LocalPlayer.Character.HumanoidRootPart.CFrame = v.CFrame
                        break
                    end
                end
            end
        end
    })
else
    local GenericTab = Window:CreateTab("Missions (Generic)", 4483362458)
    GenericTab:CreateLabel("Run this in a Bodycam game for more features.")
    GenericTab:CreateButton({
        Name = "Instant Proximity Prompts",
        Callback = function()
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("ProximityPrompt") then v.HoldDuration = 0 end
            end
        end
    })
end

local MiscTab = Window:CreateTab("Misc", 4483362458)
MiscTab:CreateButton({
    Name = "Infinite Yield",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/Edgeiy/infiniteyield/master/source'))()
    end,
})

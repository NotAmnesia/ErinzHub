local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "SPTE | Mission & Rewards Hub",
   LoadingTitle = "Hijacking Storyline...",
   KeySystem = true,
   KeySettings = {
      Title = "Executive Access",
      Note = "Key: erin01",
      Key = {"erin01"}
   }
})

-- [[ THE MISSION EXPLOIT ]]
local MissionTab = Window:CreateTab("Quests", 4483362458)

MissionTab:CreateToggle({
    Name = "Auto-Skip Missions (Massive Rewards)",
    CurrentValue = false,
    Callback = function(v)
        _G.AutoMission = v
        task.spawn(function()
            while _G.AutoMission do
                -- The remote you found
                local args = {
                    [1] = {
                        [1] = "NextMission",
                        [2] = "Main"
                    }
                }
                game:GetService("ReplicatedStorage").RemoteEvent:FireServer(unpack(args))
                
                -- Ping the server so it registers the completion
                game:GetService("ReplicatedStorage").PingEvent:FireServer()
                
                -- Fast but safe delay to prevent kick
                task.wait(0.5) 
            end
        end)
    end
})

MissionTab:CreateButton({
    Name = "Complete 10 Missions Instantly",
    Callback = function()
        for i = 1, 10 do
            local args = {[1] = {[1] = "NextMission", [2] = "Main"}}
            game:GetService("ReplicatedStorage").RemoteEvent:FireServer(unpack(args))
            task.wait(0.1)
        end
        Rayfield:Notify({Title = "Success", Content = "Skipped 10 Chapters."})
    end
})

-- [[ THE ESSENTIALS ]]
local MainTab = Window:CreateTab("Utility", 4483362458)

MainTab:CreateToggle({
    Name = "Auto-Spawn & Return (Death Anchor)",
    CurrentValue = true,
    Callback = function(v) _G.AutoRespawn = v end
})

-- [[ ENGINE OVERRIDE ]]
local function ForceSpawn()
    local pGui = game.Players.LocalPlayer:WaitForChild("PlayerGui")
    for _, v in pairs(pGui:GetDescendants()) do
        if v:IsA("TextButton") and v.Text:upper():find("SPAWN") and v.Visible then
            local x, y = v.AbsolutePosition.X + (v.AbsoluteSize.X / 2), v.AbsolutePosition.Y + (v.AbsoluteSize.Y / 2)
            game:GetService("VirtualInputManager"):SendMouseButtonEvent(x, y, 0, true, game, 0)
            task.wait(0.05)
            game:GetService("VirtualInputManager"):SendMouseButtonEvent(x, y, 0, false, game, 0)
        end
    end
end

game.Players.LocalPlayer.CharacterAdded:Connect(function(char)
    if _G.AutoRespawn then
        task.spawn(ForceSpawn)
        task.wait(1.5)
        if _G.DeathPos then char:WaitForChild("HumanoidRootPart").CFrame = _G.DeathPos end
    end
    char:WaitForChild("Humanoid").Died:Connect(function()
        if char:FindFirstChild("HumanoidRootPart") then _G.DeathPos = char.HumanoidRootPart.CFrame end
    end)
end)
